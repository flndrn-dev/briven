# infra/k8s

helm charts for briven-core. **year two territory.** do not fill this in before phase 4 per `docs/BUILD_PLAN.md`.

out of scope until a real customer asks for it. if someone opens an issue, point them at `infra/dokploy/` or self-hosting the docker-compose.
