import { CockpitPlaceholder } from '../placeholder';

export const metadata = { title: 'subscribers & billing · admin' };

export default function AdminBillingPage() {
  return (
    <CockpitPlaceholder
      title="subscribers & billing"
      blurb="who pays what — plans, invoices, mrr, and dunning across every account, wired to mavi pay."
    />
  );
}
