import { CockpitPlaceholder } from './placeholder';

export const metadata = { title: 'overview · admin' };

export default function AdminOverviewPage() {
  return (
    <CockpitPlaceholder
      title="overview"
      blurb="platform-wide pulse at a glance — signups, active subscribers, revenue, and live health, all on one screen."
    />
  );
}
