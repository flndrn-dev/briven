{
  "name": "@briven/realtime",
  "version": "0.0.0",
  "private": true,
  "description": "WebSocket service for reactive queries — LISTEN/NOTIFY fan-out",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "build": "echo 'realtime: bun runs TS directly, no build step'",
    "lint": "echo 'lint wired in phase 1'",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@briven/config": "workspace:*",
    "@briven/shared": "workspace:*",
    "postgres": "^3.4.9",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "bun-types": "^1.3.13",
    "typescript": "^6.0.3"
  }
}
