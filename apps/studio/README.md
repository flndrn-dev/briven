# @briven/studio — data browser

embedded next.js app that mounts inside the `apps/web` dashboard under `/p/:projectId/data`. read-only in phase 2 month 1, read/write in phase 2 month 3 per `docs/BUILD_PLAN.md`.

**status: skeleton.**
