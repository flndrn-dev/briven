/**
 * @briven/svelte — Svelte stores for briven.
 * Status: skeleton. Lands alongside @briven/react in Phase 1 week 7-8.
 */

export function query<_TArgs, _TResult>(_name: string, _args: _TArgs): never {
  throw new Error('briven-svelte.query: not implemented — scheduled for Phase 1 week 7-8');
}
