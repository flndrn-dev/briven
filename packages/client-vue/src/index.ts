/**
 * @briven/vue — Vue composables for briven.
 * Status: skeleton. Lands alongside @briven/react in Phase 1 week 7-8.
 */

export function useQuery<_TArgs, _TResult>(_name: string, _args: _TArgs): never {
  throw new Error('briven-vue.useQuery: not implemented — scheduled for Phase 1 week 7-8');
}
